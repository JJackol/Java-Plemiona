# Java-Plemiona
projekcik

## Requirements:

- docker
- docker-compose


### Build commands

 - `docker-compose build`
 
### Run commands

 - `docker-compose up -d`
 - `docker-compose exec front yarn install`
 - `docker-compose exec front yarn serve`
 - `docker-compose stop`
 